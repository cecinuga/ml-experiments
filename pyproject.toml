[project]
name = "ml-experiments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "thorcino",
]

[tool.uv.sources]
thorcino = { path = "../tiny-torch", editable = true }
